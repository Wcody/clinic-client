### ❌️收费列表默认列设置隐藏收费状态和操作
### ❌️挂号列表，收费列表时间过滤同时变的问题
### ❌️待诊患者，就诊列表，查询当天，查询一月内，查询一年内，就诊列表，收费列表
待诊患者，就诊列表中，查询按钮后面增加查询当天、查询一月内，查询一年等这些快捷方式，省去选择时间范围的麻烦
### ❌️接诊后，上一位病人的治疗信息没有彻底重置
就诊列表点击确诊后，会跳转到医生门诊页面，但是数据没有重置彻底，在医生门诊页面中，患者信息是切换了，但是病历信息，医嘱信息等还是显示上一位患者的。因为目前医生门诊加了路由缓存，所以目前有两种接诊方式，一种是页面没打开过的，好像没有问题，一种是页面打开过了，通过监听路由变化实现的， 有可能问题出现在此，你详细检查确认一下
### ❌️打印处方显示总金额，明细金额不用显示
### ❌️嘱托加长，其它缩短
西药处方组件中，医嘱列宽度在现有基础上增加三倍，单价列宽设置为原来的1/3，天数跟加价总宽度一致，两个单位列宽设置为原来宽度的1/2，用法列宽设置为原来宽度的1/2
### ❌️天数蓝色加粗显示，处方需要填写内容文字加粗，颜色不变
### ❌️过敏描述红色加粗显示
基本信息组件中，过敏描述内容加粗并红色显示
### ❌️调用历史处方，会复写之前的历史处方，不是新增处方
### ❌️处方历史选择框中，左边的列表倒序排序，最新的在前面，列表中展示，相邻并且日期跟上一行的相同时，不显示日期
### ❌️调用历史处方时，不能将处方id设置为undefined，这样会导致已经保存再修改的处方变成了新增了
### ❌️打印处方时，年龄没有正确显示
1、在病人信息新增生日字段birthDate
2、新增修改病人信息时，根据firstAge,ageType,lastAge字段计算出生日和age字段
3、firstAge,ageType,lastAge,age每日凌晨1点重新计算
### ❌️另存处方模板和另存病历模板的功能实现
1、病历信息中，点击另存病历模板时，先检查，当前病历模板的所有数据都为空时，弹出不允许创建空模板，否则弹出输入病历模板的名称框，点击确定后，调用后端另存病历模板的接口，接口入参就是病历模板的实体，从当前病历信息提取所需所有数据，以本地数据为准（注意病历主键ID别带上去了）
2、处方信息中，点击另存处方模板时，先检查当前处方处方列表是空时，弹出不允许创建空模板，否则弹出输入处方模板的名称框，点击确定后，调用后端另存为处方的接口，接口入参是处方模板主表和处方模板明细表的实体组合，从当前选中的处方列表中提取所需数据，以本地数据为准（注意处方主表ID，处方明细表的ID这些别带上去了）
### ❌️治疗建议同步到医嘱信息页面，保存还不行
clinic.vue中，在病历信息和医嘱信息页面都要加上治疗建议，数据都是病历主表中的treatAdvice字段，病历信息页最后加上，医嘱信息页在总金额行前面加上
病历信息，医嘱信息中，只有对应数据发生了改变才将保存按钮可以点击，否则，直接disabled，注意不是隐藏，隐藏有其它处理逻辑的，只有对应数据发生修改后，保存按钮才将disabled去掉，才允许点击保存
点击病历信息的保存时，直接保存病历
点击处方的保存时，如果病历的保存按钮亮起来了，先保存病历，再保存处方
如果是新增的方式，保存成功后记得要回写每个部分的主键ID，否则下次再次保存时还是新增的方式，尤其是更改已经存在部分处方信息的，又新增了处方的情况要特别注意
### ❌️就诊列表，收费列表，总金额后面显示诊断信息
就诊列表、收费列表的表格中，总金额后面新增诊断信息列（diagnosis）
### ❌️西药处方组件中，单次用量的单位默认已经带出药品管理表中设置的单次用量单位，并且单次用量单位改变，不能改变计价单位
### ❌️添加药品到处方组件中，默认天数为0
### 知识库+诊断+关联成方

## 诊所管理组 / 诊所管理优化待办

> 范围：聚焦平台层的诊所管理组、诊所管理，以及它们和当前诊所系统的边界。暂不纳入病案/档案、质控闭环相关模块。
> 租户上下文统一使用 `tenantId`；前端新增/改造逻辑暂时无视历史 `clinic` 字段；暂时不处理 `customerId` 链路。
> 平台层授权对象分两类：客户管理用于独立部署授权，租户管理用于 SaaS 服务授权；当前诊所系统优化优先聚焦 SaaS 租户链路。

### P0 必做：让现有功能从“能用”到“稳”

#### P0-0 补全平台层初始化数据
- [x] 建立平台层初始化数据清单，作为租户系统优化的第一步。
- [ ] 确认并初始化既有 `super` 平台管理账号，确保平台首次部署后可登录。
- [ ] 初始化平台基础角色：`super` 平台管理角色、诊所管理员角色等；不另起一套平台管理员账号概念。
- [ ] 初始化菜单资源与按钮权限资源，保证 `/menu/getRoutes` 和按钮权限有稳定数据源。
- [ ] 初始化诊所管理组根节点，例如“全部诊所”或“默认诊所管理组”。
- [ ] 初始化默认菜单套餐，例如试用版、基础版、标准版，供新建诊所时选择。
- [x] 初始化诊所基础数据模板，用于后续新建诊所时复制到对应 `tenantId`。
- [x] 平台租户下允许维护可复制的“租户数据”模板，新建 SaaS 诊所时按标识复制一份到新 `tenantId`。
- [x] 只有带 `tenantId` 的表才进入“租户数据”模板候选范围；无 `tenantId` 的关系表、视图、授权表不考虑增加该标识。
- [x] 需要为带 `tenantId` 且可复制的模板数据设计统一标识字段或标识规则，区分平台自用数据、租户初始化模板数据、普通租户业务数据。
- [x] P0 需要增加“租户数据”标识字段的表：`tb_user`、`tb_role`、`tb_dept`、`bq_medical_dictionary`、`bq_registration_fee`、`bq_additional_fee`、`bq_diagnosis_dict`、`bq_examine_item`、`bq_treatment_item`、`bq_clinic_department`、`bq_enum_item`、`bq_drug`。
- [x] P0 表统一增加“租户初始化数据”布尔字段，建议字段名 `tenantInitData`，默认值为 `true`；平台租户下 `tenantInitData = true` 的数据会在新建 SaaS 诊所时复制到新 `tenantId`。
- [x] 复制 `tb_user` 初始化模板时，必须重置账号、密码、手机号、邮箱等唯一/敏感字段，或只把它作为管理员账号创建流程的默认模板来源。
- [ ] P1/P2 按需要再增加“租户数据”标识字段的表：`bq_medical_record_template`、`bq_prescription_template`、`bq_prescription_template_detail`、`bq_clinical_practice_topic`、`bq_clinical_practice_detail`、`bq_step_by_step_section`、`bq_urgent_consideration_section`。
- [x] 明确不需要“租户数据”标识字段的表：`bq_clinic_staff`、`sys_*` 关系表、交易/流水类业务表、`tb_customer` 及 `sys_customer_*` 独立部署授权链路。
- [x] 基础数据模板至少包含：用户模板、角色、部门、科室、挂号项、附加费、诊断字典、药品、药品单位、用法、频次、煎药方式、常用参数。
- [ ] 初始化系统参数，明确哪些参数属于平台级，哪些参数会下发为诊所级默认值。
- [ ] 初始化关于页面/授权页面的平台信息默认数据。
- [x] 为平台初始化提供可重复执行的幂等机制，避免重复初始化造成脏数据。
- [x] 为平台初始化提供版本号或迁移标识，方便后续升级补数据。
- [ ] 提供平台初始化结果检查页或接口，能看出哪些初始化项已完成、哪些缺失。

#### P0-1 统一命名和层级边界
- [x] 全面梳理前端文案，统一使用“诊所管理组”和“诊所管理”。
- [x] 将页面中的“租组”统一调整为“诊所管理组”或更清晰的业务名。
- [x] 修复诊所管理里残留的“角色名称”等不准确提示文案。
- [x] 修复表单 placeholder 中“请输入登录账号”“请输入用户姓名”等复制残留文案。
- [x] 梳理平台层与租户层命名边界：平台层管理诊所，租户层处理当前诊所业务。
- [x] 明确客户管理与诊所管理的业务边界：客户管理是独立部署授权对象，诊所管理是 SaaS 租户授权对象。

#### P0-2 改造诊所管理组类型判断
- [x] 当前 `src/views/system/group/index.vue` 通过 `route.fullPath` 判断组类型，改为路由 `meta.kind` 或显式 props。
- [x] 将 `initKind = 0` 明确标注为诊所管理组，`initKind = 1` 明确标注为客户管理组。
- [x] 避免后续路由路径变化导致诊所组/客户组判断失效。

#### P0-3 修复分页和查询行为
- [x] `src/views/system/tenant/utils/hook.tsx` 中 `handleSizeChange` 实现页大小变更后刷新列表。
- [x] `src/views/system/tenant/utils/hook.tsx` 中 `handleCurrentChange` 实现页码变更后刷新列表。
- [x] 检查诊所管理组页面是否需要分页或树形数据刷新逻辑补强。
- [x] 查询条件重置后保持左侧组树选择状态和标题一致。

#### P0-4 增强诊所列表关键字段
- [x] 诊所列表增加授权类型展示。
- [x] 诊所列表增加到期时间展示，并对已过期/即将到期做视觉提示。
- [x] 诊所列表增加最大用户数展示。
- [x] 诊所列表增加当前用户数展示（需要后端字段或接口支持）。
- [x] 诊所列表增加管理员数量展示（需要后端字段或接口支持）。
- [x] 诊所列表增加菜单授权状态或套餐名称展示（需要后端字段或接口支持）。

#### P0-4A 新建诊所必须包含初始化
- [x] 新建诊所时，后端需要支持创建 `tenantId` 后的初始化流程，不只是保存诊所基础资料。
- [x] 新建诊所初始化依赖 P0-0 中的平台默认菜单套餐、默认角色和基础数据模板。
- [x] 初始化诊所管理员账号：支持填写管理员账号、姓名、手机号、初始密码或自动生成初始密码。
- [x] 管理员账号必须绑定到新建诊所的 `tenantId`。
- [x] 初始化管理员角色与权限：至少创建/绑定诊所管理员角色，确保首次登录可用。
- [x] 初始化诊所默认菜单权限：新诊所必须有可登录后的基础菜单集合。
- [x] 初始化诊所基础数据：科室、挂号项、附加费、诊断字典、药品单位/用法/频次等基础字典按默认模板初始化。
- [x] 新建诊所初始化时，从平台租户下标记为“租户数据”的模板行复制到新 `tenantId`，复制时重置主键、创建人、创建时间等审计字段。
- [x] 初始化结果需要前端明确反馈：成功、部分成功、失败原因，以及是否需要重试初始化。
- [x] 新建诊所失败或初始化失败时，要考虑回滚或提供“重新初始化”入口。

#### P0-5 删除和停用保护
- [x] 删除诊所管理组前，检查是否存在子组。
- [x] 删除诊所管理组前，检查是否存在归属该组的诊所。
- [x] 删除诊所前，检查是否存在管理员/用户绑定。
- [x] 删除诊所前，检查是否存在业务数据，至少给出明确风险提示。
- [x] 停用诊所前，弹窗说明会影响该诊所用户登录和业务访问。
- [x] 对批量删除增加更明确的确认信息，列出数量和影响范围。

#### P0-6 修复认证与上下文基础问题
- [x] 检查 `src/utils/auth.ts` 中 `refreshToken` 当前取 `data.accessToken` 是否为 bug。
- [x] 登录态中补充并保存 `tenantId`、`tenantName`、`tenantLogo` 等当前诊所上下文。
- [x] 租户上下文统一使用 `tenantId`，前端新增逻辑不再使用 `clinic/clinicId` 作为租户识别字段。
- [x] 梳理现有 `clinic` 字段出现位置，标记为历史兼容字段，避免继续扩散。
- [x] 暂时不改造 `customerId` 链路；保留其作为独立部署授权链路，SaaS 诊所链路先依赖角色、菜单和 `tenantId`。
- [x] 统一导航栏诊所信息来源，避免同时从登录态和 `/tenant/getInfo` 两套来源取值导致不一致。

#### P0-7 关于界面区分平台层与租户层
- [x] 当前 `src/views/about/index.vue` 的“关于系统”需要根据登录身份区分平台层和租户层展示。
- [x] 平台层账号查看时，标题建议为“关于平台”或“平台信息”，展示系统名称、系统版本、版权声明、平台授权、客户/诊所配额概览等。
- [x] 租户层账号查看时，标题建议为“关于诊所”或“诊所信息”，展示当前诊所名称、诊所 Logo、授权类别、到期时间、用户额度、可用模块等。
- [x] 避免租户层用户看到平台级设备码、注册码等敏感授权字段，除非该角色具备平台授权查看权限。
- [x] 将 `src/views/auth/systemAuthInfo.tsx` 中的展示字段拆分为平台信息和诊所信息两套配置。
- [x] 评估 `/auth` 系统授权信息页面是否也需要按平台层/租户层拆分展示。
- [x] 补充后端接口或字段来源：平台授权信息、当前诊所授权信息、当前诊所额度与模块授权信息。

### P1 建议优化：把管理流程做顺

#### P1-1 新建诊所向导
- [ ] 将新增诊所拆为分步流程：基本信息、归属诊所管理组、初始化管理员账号、选择菜单授权、初始化基础数据。
- [ ] 新建成功后给出后续动作入口：继续新增、进入详情、设置菜单、设置管理员。
- [ ] 支持从诊所管理组节点直接新建诊所，并自动带入当前组。
- [ ] 初始化管理员账号步骤支持“创建新管理员”和“选择已有用户作为管理员”。
- [ ] 初始化基础数据步骤支持选择初始化模板，例如默认诊所模板、空白模板、自定义模板。
- [ ] 新建完成页展示管理员账号、登录诊所、初始密码提示、初始化数据摘要。

#### P1-2 诊所详情抽屉/详情页
- [ ] 增加诊所详情入口。
- [ ] 详情中展示基本资料、授权信息、管理员、菜单权限、用户额度、创建时间、状态。
- [ ] 详情中展示最近操作记录。
- [ ] 在详情中集中提供启停、续期、改组、设置管理员、设置菜单等操作。

#### P1-3 菜单授权体验升级
- [x] 将当前右侧菜单树面板补充“已选数量”和“半选提示”。
- [ ] 支持菜单树搜索后保留勾选状态。
- [ ] 支持一键展开/收起、全选/反选、恢复默认。
- [ ] 引入“菜单套餐”概念：基础版、标准版、旗舰版、试用版。
- [ ] 新建诊所优先选择套餐，必要时再微调菜单树。

#### P1-4 管理员绑定优化
- [x] 设置管理员弹窗增加搜索用户能力。
- [ ] 区分 `super` 平台管理账号与诊所管理员；暂不展开客户管理员链路。
- [x] 显示当前已绑定管理员数量和名称。
- [x] 支持解绑确认，并说明解绑后的登录影响。

#### P1-5 批量操作
- [x] 支持批量启用/停用诊所。
- [ ] 支持批量移动到诊所管理组。
- [ ] 支持批量设置菜单套餐。
- [ ] 支持批量设置到期时间或续期。
- [ ] 支持批量导出诊所清单。

#### P1-6 操作审计
- [ ] 记录诊所新增、修改、停用、删除、改组。
- [ ] 记录管理员绑定/解绑。
- [ ] 记录菜单授权变更。
- [ ] 在诊所详情中展示审计时间线。

### P2 产品化增强：让平台层更像平台

#### P2-1 诊所运营看板
- [ ] 平台首页或诊所管理页增加统计卡片：诊所总数、启用数、停用数、即将到期数、已过期数。
- [ ] 增加用户额度使用率统计。
- [ ] 增加按诊所管理组统计诊所数量。
- [ ] 支持点击统计卡片快速筛选列表。

#### P2-2 到期与配额预警
- [ ] 支持到期前 N 天预警。
- [ ] 支持用户数接近上限预警。
- [ ] 诊所停用或过期时，租户端展示明确原因。
- [ ] 平台层提供续期入口。

#### P2-3 配置继承
- [ ] 诊所管理组支持默认菜单套餐。
- [ ] 诊所管理组支持默认角色模板。
- [ ] 诊所管理组支持默认基础参数。
- [ ] 新建诊所时自动继承组配置，并允许覆盖。

#### P2-4 公共组织管理基座
- [ ] 抽象 `customer` 和 `tenant` 管理页的公共列表、树、菜单授权、管理员绑定逻辑。
- [ ] 将差异点配置化：标题、接口、权限码、字段、菜单动作。
- [ ] 减少 `customer/utils/hook.tsx` 与 `tenant/utils/hook.tsx` 的重复代码。

#### P2-5 前端租户上下文模型
- [ ] 建立统一 `tenantContext` store。
- [ ] `tenantContext` 的主键字段统一为 `tenantId`。
- [ ] 对历史接口返回的 `clinic` 字段只做兼容读取，不作为新的上下文字段暴露。
- [ ] 当前诊所业务页面统一从 `tenantContext` 读取诊所信息。
- [ ] 平台管理页面显式区分“正在管理的诊所”和“当前登录诊所”。
- [ ] 为后续租户切换、调试、审计打基础。
